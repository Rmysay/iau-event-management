# Features

Feature-based modules for larger functionality slices.

Planned modules:
- `events/`       — event CRUD logic, filters, state
- `registrations/` — booking flow, confirmation emails
- `auth/`         — login, token management, guards
